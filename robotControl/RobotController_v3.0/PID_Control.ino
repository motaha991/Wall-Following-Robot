//#include <PID_v1.h>
/************************ PID Definitions and Variables **************************/
// defines variables
/*double current_distance;   // Input to myPID
double prev_distance = 0;
double PID_output;          // Output of myPID
double reference_distance;  // Setpoint: desired distance from the wall
double deviation_error;     // difference between current_distance and the desired distance

//Specify the links and initial tuning parameters
double Kp = 50.5, Ki = 10.0, Kd = 25.0;//15.5;
int SampleTime = 100;

PID myPID(&current_distance, &PID_output, &reference_distance, Kp, Ki, Kd, DIRECT);

void PID_initiallize()
{
  myPID.SetSampleTime(SampleTime);
  myPID.SetOutputLimits(-350.0,350.0);  // 25% of the robot's Motor velocities
}*/
